def permu(str):
  b= ''.join(sorted(str))
  return b

def final_check(a,b):
    
  if (len(a)!=len(b)):
    return False
  else:
    a_sort = permu(a)
    b_sort = permu(b)
    if (a_sort == b_sort):
      return True
    else:
      return False
 
print(final_check("abcd","cabn"))

